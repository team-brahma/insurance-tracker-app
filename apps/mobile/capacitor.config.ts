import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.brahma.insurancetracker',
  appName: 'Insurance Tracker',
  webDir: 'dist',
};

export default config;
